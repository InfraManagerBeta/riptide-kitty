{
  "name": "riptide-kitty",
  "version": "0.1.0",
  "private": true,
  "description": "Riptide Kitty — spec-001 rigged animated kitty: validator, self-test, and zero-build viewer (no runtime dependencies, no build step).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-kitty.mjs",
    "test": "node --test scripts/test-validate-kitty.mjs"
  }
}
